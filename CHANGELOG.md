# 版本变化记录

本项目从首次公开版本开始记录面向用户的重要变化。

## 1.0.0

### 新增

- 拖拽排序、1v1 传递排序、1v1 矩阵排序和滑杆排序四种工作方式；
- 多排序任务、初始顺序、并列组、局部重排、撤销与重做；
- 自动保存、项目快照、异常退出恢复、结果锁定与解锁；
- 排名与评分写入，以及 CSV、JSON、Markdown 和纯文本导出；
- 本地图片匹配和显式开启的网络图片预览。

### 安全与兼容性

- 建立 v1.0.0 项目数据库基线，内部格式版本为 `10000`；
- 不再读取或迁移开发预览版及 v1.0.0 之前的项目结构；
- 为应用 WebView 启用内容安全策略，并保持最小 Tauri 桌面权限；
- 添加持续集成、依赖更新、安全报告和贡献指南。
