# 安全策略

## 受支持版本

当前仅对最新的 `1.0.x` 正式版本提供安全修复。开发预览版及 v1.0.0 之前创建的项目格式不在支持范围内。

## 私下报告漏洞

请使用 GitHub 仓库 **Security → Report a vulnerability** 的[私密漏洞报告页面](https://github.com/Pan1corn/PersonalRanker/security/advisories/new)。不要在公开 Issue、讨论区或拉取请求中披露尚未修复的利用细节。

报告中请尽量包含：

- 受影响的应用版本和 Windows 版本；
- 可重复的最小操作步骤；
- 预期行为、实际行为和潜在影响；
- 必要的日志或最小化测试文件。

请勿提交真实的 `.subject-sort` 项目、私人数据、访问令牌或其他敏感信息。若漏洞只在特定数据上出现，请制作不含隐私信息的最小复现项目。

维护者会尽量在 7 天内确认报告，并在完成影响评估后协调修复和披露时间。确认漏洞修复并发布新版本之前，请保持报告内容私密。

## 安全边界

- 应用默认只处理本地文件，不提供遥测、账户或云同步功能。
- 网络图片需要用户显式开启；开启后会由系统 WebView 向图片地址发送请求。
- Tauri IPC 仅暴露应用所需命令，桌面能力配置保持最小权限。
- 项目目录中的 SQLite 数据库、来源副本、媒体资源和历史快照应视为用户数据，报告问题时不得公开上传。

普通功能缺陷、界面问题和不涉及安全影响的崩溃可以使用公开 Issue 报告。
